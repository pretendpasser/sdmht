ENV= LOG_LEVEL='debug'
LOG_DISABLE_SAMPLING= LOG_DISABLE_SAMPLING='true'
WS_LISTEN_ADDR= WS_LISTEN_ADDR='0.0.0.0:4090'
GRPC_LISTEN_ADDR= GRPC_LISTEN_ADDR='0.0.0.0:7091'
MNG_LISTEN_ADDR= MNG_LISTEN_ADDR='0.0.0.0:7090'
# ACCOUNT_ACCESS_ADDR= 0.0.0.0:17001
HEARTBEAT_INTERVAL= HEARTBEAT_INTERVAL=3600

run:
	$(ENV) $(LOG_DISABLE_SAMPLING) $(WS_LISTEN_ADDR) $(GRPC_LISTEN_ADDR) $(MNG_LISTEN_ADDR) $(HEARTBEAT_INTERVAL) go run app/sdmht_conn/main.go